import path from 'path';

export const DEFAULT_DOWNLOADS_DIR = path.join(process.cwd(), 'meeting-bot', 'downloads');
export const DEFAULT_OUTPUT_DIR = path.join(process.cwd(), 'meeting-bot', 'output');
