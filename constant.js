import path from 'path';

export const DEFAULT_DOWNLOADS_DIR = path.join(process.cwd(), 'meeting-scribe', 'downloads');
export const DEFAULT_OUTPUT_DIR = path.join(process.cwd(), 'meeting-scribe', 'output');
